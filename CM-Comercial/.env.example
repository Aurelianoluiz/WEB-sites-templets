APP_ENV=production
APP_URL=https://www.seudominio.com.br
APP_NAME=CM Comercial
PAYMENT_GATEWAY=manual

# Mercado Pago — configure only on the server/environment, never commit real values.
MP_ACCESS_TOKEN=
MP_WEBHOOK_SECRET=
# Optional: webhook signature freshness window in seconds (default: 300).
MP_WEBHOOK_MAX_SKEW=300

# Nunca coloque segredos ou números de cartão no repositório.
